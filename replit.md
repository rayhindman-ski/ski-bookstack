# ski-bookstack

A ski wiki project.

## Setup

- Runtime: Node.js 20
- Dev server: Vite on port 5000
- Start: `npm run dev`

## Notes

- Project is in early stage; `index.html` is a minimal placeholder.
- `feature/init-docker` branch creation was requested but requires a project task (destructive git op).
